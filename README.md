# This project is just a test to mess around with react, go, and WebSockets.

## backend
To run locally, create inside the `backend` folder a file: `.envrc` with content:

```
export CORS=http://localhost:3000
```

Then run 
```
make run/api
```

## frontend

```
npm i
npm start
```